#include<iostream>
#include<cmath>
using namespace std;
int main(){
	int x,y;
	cout<<"Nhap x: ";cin>>x;
	cout<<"Nhap y: ";cin>>y;
	cout<<"a) F= "<<(pow(x,5))+(5*pow(x,(5.0)/3))-6<<endl;
	cout<<"b) G= "<<(x*y)-(pow(x,2))+(x*pow(y,2))-(pow(y,3));
	
	return 0;
}
